enum Samples {
  nativeVideoPlayer('Native Video Player', 'A native video player implementation');

  final String displayName;
  final String description;

  const Samples(this.displayName, this.description);
}
